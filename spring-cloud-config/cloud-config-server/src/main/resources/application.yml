server:
  port: 8888

spring:
  application:
    name: "config-server"

  cloud:
    config:
      server:
        git:
          uri: "https://github.com/yeob32/spring-cloud-sample/cloud-config-repo"

management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /actuator