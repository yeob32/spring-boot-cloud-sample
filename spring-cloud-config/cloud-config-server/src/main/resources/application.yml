spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yeob32/spring-cloud-config-repository

management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /actuator